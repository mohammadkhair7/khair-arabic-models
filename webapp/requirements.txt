# Web layer only. The models themselves come from the parent package
# (`pip install -e ..`), which pulls in torch.

# ...except in the container, where the parent package is installed with
# --no-deps so that pip cannot replace the CPU-only torch wheel with the
# 2 GB CUDA build from PyPI. That also drops numpy, which arabicmodels
# imports at module scope, so it has to be named here or the image builds,
# starts, and dies on the first model load with ModuleNotFoundError.
numpy>=1.24

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9      # multipart/form-data uploads
httpx>=0.27                  # SendGrid call, and the test client

# input formats
openpyxl>=3.1                # .xlsx
python-docx>=1.1             # .docx
olefile>=0.47                # .doc (legacy OLE compound files)

# output formats
fpdf2>=2.7                   # PDF
arabic-reshaper>=3.0         # letter joining for PDF
python-bidi>=0.4             # right-to-left reordering for PDF

# Optional but strongly recommended: with uharfbuzz installed, fpdf2 shapes
# and line-breaks Arabic itself, which is more correct than the
# reshaper + bidi fallback for mixed-direction text.
uharfbuzz>=0.39
